# App 参数照片复核

状态：PARTIALLY COMPLETE；硬件标签 ID 尚未确认。

本轮用户照片可见：坐标偏航角 9.0、标签 ID 1.0、标签总数 1.0；四基站位置分别为 (90,-90,256)、(0,500,271)、(400,500,271)、(400,0,271)。数值与历史厘米制坐标及偏角一致。照片为参数页，没有四路实时距离、各基站在线状态或槽位实际来源信息。

历史来源 `docs/2026-09-04_record_flight-controller-ui-parameters-v02.md` 明确记载：用户要求将照片显示的标签 ID 1 改记为实际使用的 2。本轮单独提供照片不等于撤销该明确说明，因此不改配置，也不把截图显示值视为已读回的硬件生效值。

代码核查：

- `uav_readonly_adapter_v2.py` main 以 `uwb_tag_id=2` 创建 AdapterConfig。
- `uav_readonly_adapter.py` 将配置 ID 写入输出状态；四槽距离另取自 BATTERY_STATUS.voltages[2:6]。
- `telemetry_codec.py` 按 expected_tag_id 过滤 JSON 标签。这只能核对配置赋值，不能独立验证硬件标签身份。

因此现有 JSON 的 tag_id=2 不是硬件 ID 的实测证据。尚无依据将标签身份差异认定为米级异常的根因；之前原始四槽的三角不等式矛盾也不因 JSON 标签改名而消失。

本轮验证为照片人工转录、历史记录与上述源代码交叉核查；未执行在线参数读回。仅新增本记录，未改源码、飞控参数、基站坐标或 9° 偏角。需确认这张照片是当前连接后的参数还是此前照片，实际标签 ID 是否仍为 2。未扩大任务范围。
