# E048 现有图像的单次近似目标坐标计算

状态：COMPUTED / APPROXIMATE；不是实时全链路 VALIDATED。

用户确认现有居中目标为 3 号路口红绿灯，并要求直接用现有图像算一次坐标。本次仅新增离线计算和结果，不修改、发布或覆盖在线定位状态。

输入为 E047 已保存的 centered_v09 图像参考和实际图像特征、平台位姿事件。参考中较高绿色立柱顶端像素选为 (555,794)，采用 E030 用户量测的高灯高度 0.22 m。因为该顶端对应的原始锚点未在后续帧中直接被观测，使用已匹配物体特征的局部单应矩阵转移该像素，属于额外近似；不能把它称为直接测得顶端像素。图像尺寸和 ROI 内参保持 E028 实际标定配置，平台和相机外参沿用 E047 近似值。

从已有 7 次有效实例观测中，3 帧满足双侧平台位姿时间间隔不超过 250 ms、单应几何内点至少 8 个以及射线与高度平面正向相交的条件。按几何内点数量最高、时间括号最小选取一次结果，没有按靠近路口目录坐标筛选。

所选图像时刻 1788869810662535244 ns，目标像素约 (369.974,808.821)，10 个单应内点，平台时间括号约 195.20 ms。平台位置约 (0.74393,4.62062,0.74399) m。

目标顶端近似结果，在 E047 在线 map 中：

**(x,y,z) = (0.75908, 4.75500, 0.22000) m**。

z=0.22 m 是高度先验，不是从本次图像独立测出的高度。以同一条射线改用低灯高度 0.12 m，得到 (0.76197,4.78065,0.12) m，仅用于高度敏感性说明，不表示另一个目标的实测位置。

计算：d_map=R_map_body R_body_camera K_rect^-1 [u,v,1]；P=C+(h-C_z)/d_z * d。平台位置线性插值，姿态 SLERP 插值。用反向投影与高度交点断言核对数值实现，误差约 4.1e-13 像素只代表代数自洽，绝不代表物理精度。

复跑：在仓库根目录运行 `python3 experiments/runs/2026-09-08_E048_single_frame_target_coordinate/calculate.py`。输入事件及标定 SHA256、脚本 SHA256、Git 版本和三帧结果见 result.json。工作区为预先存在的脏状态，没有提交代码。

限制：E030 的 physical_sandbox_map_v01 与 E047 在线 map 尚无已验证的完整配准。因此用户给出的“3 号路口”只标注身份，不作为坐标拟合依据；上述数值不能宣称已落在 E030 的 3 号路口目录位置。局部单应、目标高度、相机安装、UWB 误差与平台插值都会影响结果。未知具体路口角点，本次不擅自指定 J03 的 NW/NE/SE/SW。没有取得新的红绿灯类别识别证据。

未扩大任务范围：只执行用户要求的一次离线近似坐标计算；未改在线算法、未发送飞行指令。
