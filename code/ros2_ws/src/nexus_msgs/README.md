# nexus_msgs

ROS2 message contracts for external target localization. `TargetObservation` always represents the external target, never the drone platform pose. Real target IDs, frames, extrinsics and source data remain TBD until hardware acceptance.
